import ConfigButtonComponent from '../../component/config/ConfigButtonComponent';

import {
  Story,
  Props,
  Description,
  Preview
} from '@storybook/addon-docs/blocks';

# Button

<Description of={ConfigButtonComponent} />

## Props

컴포넌트에서 사용되는 Props
(기본적으로 reactstrap에 넘기는 props는 다 가능 합니다.)

<Props of={ConfigButtonComponent} />
